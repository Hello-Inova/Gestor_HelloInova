// Lista única de tipos de sistema, compartilhada entre as rotas de
// sistemas e do dashboard (evita duas listas que podem ficar dessincronizadas).
const SYSTEM_CATEGORIES = ['Web Site', 'Landing Page', 'Catálogo Digital', 'ERP', 'SAAS', 'Holding H.I'];

module.exports = { SYSTEM_CATEGORIES };
